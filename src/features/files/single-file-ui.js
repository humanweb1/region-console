import { store } from "../../state/store.js";
import { upsertState } from "../../services/cloud.js";
import { getElements, openDialog, closeDialog, toast } from "../../components/shell.js";
import { importRegionData } from "../regions/importer.js";

const elements = getElements();
const TYPES = { country: "Ülke", province: "İl", district: "İlçe", neighborhood: "Mahalle", cemetery: "Mezarlık" };
const FOLDERS = { district: "İlçeler", neighborhood: "Mahalleler", cemetery: "Mezarlıklar" };
const norm = (v) => String(v ?? "").trim().toLocaleLowerCase("tr-TR").replace(/ı/g,"i").replace(/ğ/g,"g").replace(/ü/g,"u").replace(/ş/g,"s").replace(/ö/g,"o").replace(/ç/g,"c");
const esc = (v) => String(v ?? "").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
const files = () => Array.isArray(store.get().regions?.custom) ? store.get().regions.custom.filter((r) => r?.id) : [];
const catalog = () => Array.isArray(window.RegionConsoleRBAC?.access?.regionCatalog) ? window.RegionConsoleRBAC.access.regionCatalog : [];
const typeOf = (r) => String(r?.hierarchy?.type || r?.type || "independent").toLowerCase();
const nameOf = (r) => String(r?.name || r?.title || "İsimsiz").trim();
const idOf = (r) => String(r?.id || "");
const parentIdOf = (r) => r?.hierarchy?.parentId ?? r?.parent_id ?? null;
const allNodes = () => [...catalog(), ...files()];
const findNode = (id) => allNodes().find((r) => idOf(r) === String(id)) || null;
function ancestors(r) { const out=[]; const seen=new Set(); let x=r; while(x&&!seen.has(idOf(x))){seen.add(idOf(x));out.push(x);x=findNode(parentIdOf(x));} return out; }
function pathOf(r) { const h=r?.hierarchy||{}; const c=h.countryName||ancestors(r).find(x=>typeOf(x)==="country")?.name||"Turkey"; const p=h.provinceName||ancestors(r).find(x=>typeOf(x)==="province")?.name; const d=h.districtName||ancestors(r).find(x=>typeOf(x)==="district")?.name; const n=h.neighborhoodName||ancestors(r).find(x=>typeOf(x)==="neighborhood")?.name; const t=typeOf(r); if(t==="country")return[c]; if(t==="province")return[c,p||nameOf(r)]; if(t==="district")return[c,p||"İl",FOLDERS.district,d||nameOf(r)]; if(t==="neighborhood")return[c,p||"İl",FOLDERS.district,d||"İlçe",FOLDERS.neighborhood,n||nameOf(r)]; if(t==="cemetery")return[c,p||"İl",FOLDERS.district,d||"İlçe",...(n?[FOLDERS.neighborhood,n]:[]),FOLDERS.cemetery,nameOf(r)]; return[nameOf(r)]; }
function catalogTree() { const root={}; const folders=(node,name)=>node[name] ||= {}; for(const c of catalog().filter(r=>typeOf(r)==="country")){let n=folders(root,nameOf(c)); for(const p of catalog().filter(r=>typeOf(r)==="province"&&String(r.parent_id||"")===idOf(c))){let pn=folders(n,nameOf(p)); const dr=folders(pn,FOLDERS.district); for(const d of catalog().filter(r=>typeOf(r)==="district"&&String(r.parent_id||"")===idOf(p))){let dn=folders(dr,nameOf(d)); folders(dn,FOLDERS.neighborhood); folders(dn,FOLDERS.cemetery);}}} return root; }
function insertTree(node, parts, file) { let n=node; for(const part of parts) n=n[part] ||= {}; n.__file=file; }
function renderTree(node, depth=0) { return Object.entries(node).filter(([k])=>k!=="__file").sort(([a],[b])=>a.localeCompare(b,"tr")).map(([name,child])=>{const file=child.__file; const inner=renderTree(child,depth+1); const card=file?`<div class="files-list"><article class="file-card"><div class="file-main"><strong>${esc(nameOf(file))}</strong><span>${TYPES[typeOf(file)]||"Dosya"} · ${file.geometry?"Geometri mevcut":"Boş"}</span><small>${esc(nameOf(file))}</small></div><button class="button file-delete" type="button" data-file-id="${esc(file.id)}">Sil</button></article></div>`:""; return `<details class="file-folder" data-folder-search="${esc(name)}"><summary><span class="file-folder-name">📁 ${esc(name)}</span></summary><div class="file-folder-content">${card}${inner}</div></details>`;}).join(""); }
function buildTree() { const root=catalogTree(); for(const file of files()) insertTree(root,pathOf(file),file); return root; }
async function persist() { const token=store.get().auth.session?.access_token; if(!token)return; await upsertState(token,{...store.dataSnapshot().regions,campaigns:store.get().campaigns,mapSettings:store.get().mapSettings}); }
function optionList(type,selected={}) { const wanted=type==="country"?null:type==="province"?selected.country:type==="district"?selected.province:type==="neighborhood"?selected.district:selected.neighborhood; return catalog().filter(r=>typeOf(r)===type && (!wanted || ancestors(r).some(a=>idOf(a)===String(wanted)))).sort((a,b)=>nameOf(a).localeCompare(nameOf(b),"tr")).map(r=>`<option value="${esc(r.id)}">${esc(nameOf(r))}</option>`).join(""); }
function newFile() { openDialog(elements,"Yeni dosya",`<form id="singleNewFile"><p class="dialog-muted">Dosya yalnızca bu hiyerarşi kaydında oluşturulur. Geometri başlangıçta boş olabilir.</p><label>Tür<select id="sfType" required><option value="">Seçin</option>${Object.entries(TYPES).map(([k,v])=>`<option value="${k}">${v}</option>`).join("")}</select></label><label id="sfCw" hidden>Ülke<select id="sfC"></select></label><label id="sfPw" hidden>İl<select id="sfP"></select></label><label id="sfDw" hidden>İlçe<select id="sfD"></select></label><label id="sfNw" hidden>Mahalle<select id="sfN"></select></label><label>Dosya adı<input id="sfName" required></label><div class="dialog-actions"><button type="button" class="button" id="sfCancel">İptal</button><button class="button button-primary">Dosya oluştur</button></div></form>`); const q=id=>elements.dialogBody.querySelector(id),t=q("#sfType"),c=q("#sfC"),p=q("#sfP"),d=q("#sfD"),n=q("#sfN"),name=q("#sfName"); const sync=()=>{const v=t.value;c.innerHTML='<option value="">Ülke seçin</option>'+optionList("country");p.innerHTML='<option value="">İl seçin</option>'+optionList("province",{country:c.value});d.innerHTML='<option value="">İlçe seçin</option>'+optionList("district",{province:p.value});n.innerHTML='<option value="">Mahalle seçin</option>'+optionList("neighborhood",{district:d.value});q("#sfCw").hidden=!v||v==="country";q("#sfPw").hidden=!['district','neighborhood','cemetery'].includes(v);q("#sfDw").hidden=!['neighborhood','cemetery'].includes(v);q("#sfNw").hidden=v!=="cemetery";}; t.onchange=sync;c.onchange=()=>{p.innerHTML='<option value="">İl seçin</option>'+optionList("province",{country:c.value})};p.onchange=()=>{d.innerHTML='<option value="">İlçe seçin</option>'+optionList("district",{province:p.value})};d.onchange=()=>{n.innerHTML='<option value="">Mahalle seçin</option>'+optionList("neighborhood",{district:d.value})};q("#sfCancel").onclick=()=>closeDialog(elements);q("#singleNewFile").onsubmit=async e=>{e.preventDefault();const v=t.value,title=name.value.trim(),ids={country:c.value,province:p.value,district:d.value,neighborhood:n.value};if(!v||!title)return;const parent=v==="country"?null:findNode(ids[v==="province"?"country":v==="district"?"province":v==="neighborhood"?"district":"neighborhood"]);if(v!=="country"&&!c.value)return toast(elements,"Ülke seçin.");if(['district','neighborhood','cemetery'].includes(v)&&!p.value)return toast(elements,"İl seçin.");if(['neighborhood','cemetery'].includes(v)&&!d.value)return toast(elements,"İlçe seçin.");if(v==="cemetery"&&!n.value)return toast(elements,"Mahalle seçin.");if(files().some(r=>typeOf(r)===v&&norm(nameOf(r))===norm(title)&&String(parentIdOf(r)||"")===String(parent?.id||"")))return toast(elements,"Aynı konumda aynı isimli dosya zaten var.");const now=new Date().toISOString(),region={id:`file-${crypto.randomUUID()}`,name:title,title,type:v,geometry:null,status:"service",hierarchy:{type:v,label:TYPES[v],level:{country:0,province:1,district:2,neighborhood:3,cemetery:4}[v],parentType:v==="country"?null:typeOf(parent),parentId:parent?.id||null,parentName:parent?.name||null,countryId:v==="country"?null:(findNode(c.value)?.id||null),countryName:v==="country"?title:(findNode(c.value)?.name||null),rootType:"country"},createdAt:now,updatedAt:now,importMeta:{sourceType:"file",sourceFile:null}};const before=store.dataSnapshot();store.replaceData({regions:{...store.get().regions,custom:[...files(),region],selectedId:null},campaigns:store.get().campaigns},{recordHistory:false});store.recordHistory(`Dosya oluşturuldu: ${title}`,before,store.dataSnapshot());try{await persist();closeDialog(elements);showFiles();toast(elements,`${TYPES[v]} dosyası oluşturuldu.`)}catch(err){toast(elements,`Bulut kaydı başarısız: ${err.message}`)}};sync(); }
function geometryOf(input){const result=importRegionData(input,"",null);const geoms=result.regions.custom.map(r=>r.geometry).filter(Boolean);if(!geoms.length)throw new Error("Geçerli Polygon veya MultiPolygon bulunamadı.");const polys=[];for(const g of geoms){if(g.type==="Polygon")polys.push(g.coordinates);else if(g.type==="MultiPolygon")polys.push(...g.coordinates);}return polys.length===1?{type:"Polygon",coordinates:polys[0]}:{type:"MultiPolygon",coordinates:polys};}
function boundsOf(g){const pts=g?.type==="Polygon"?g.coordinates.flat():g?.type==="MultiPolygon"?g.coordinates.flat(2):[];if(!pts.length)return null;const xs=pts.map(p=>p[0]),ys=pts.map(p=>p[1]);return[[Math.min(...ys),Math.min(...xs)],[Math.max(...ys),Math.max(...xs)]];}
function chooseImportTarget(file){return new Promise(resolve=>{const list=files().sort((a,b)=>pathOf(a).localeCompare(pathOf(b),"tr"));if(!list.length){toast(elements,"Önce mevcut bir dosya oluşturun.");resolve(null);return;}openDialog(elements,"Mevcut dosyaya içe aktar",`<form id="sfImport"><p class="dialog-muted">Yeni kayıt oluşturulmaz. Seçilen dosyanın yalnızca geometrisi güncellenir.</p><label>Hedef dosya<select id="sfTarget" required><option value="">Dosya seçin</option>${list.map(r=>`<option value="${esc(r.id)}">${esc(pathOf(r).join(" / "))}</option>`).join("")}</select></label><div class="dialog-actions"><button type="button" class="button" id="sfICancel">İptal</button><button class="button button-primary">Dosyaya kaydet</button></div></form>`);const form=elements.dialogBody.querySelector("#sfImport");form.querySelector("#sfICancel").onclick=()=>{closeDialog(elements);resolve(null)};form.onsubmit=e=>{e.preventDefault();resolve(list.find(r=>idOf(r)===form.querySelector("#sfTarget").value)||null);closeDialog(elements)};});}
async function importFile(){const input=document.createElement("input");input.type="file";input.accept=".json,.geojson,application/json,application/geo+json";input.onchange=async()=>{const file=input.files?.[0];if(!file)return;try{const parsed=JSON.parse(await file.text());const target=await chooseImportTarget(file);if(!target)return;const geometry=geometryOf(parsed),before=store.dataSnapshot(),now=new Date().toISOString(),custom=files().map(r=>idOf(r)===idOf(target)?{...r,geometry,bounds:boundsOf(geometry),updatedAt:now,importMeta:{...(r.importMeta||{}),geometrySourceFile:file.name,geometryImportedAt:now,format:"GeoJSON",coordinateOrder:"lonlat"}}:r);store.replaceData({regions:{...store.get().regions,custom,selectedId:target.id},campaigns:store.get().campaigns},{recordHistory:false});store.recordHistory(`Geometri dosyaya aktarıldı: ${nameOf(target)}`,before,store.dataSnapshot());await persist();document.dispatchEvent(new CustomEvent("region-console:state-changed"));showFiles();toast(elements,`${file.name} → ${nameOf(target)}`)}catch(err){console.error("[Region Console] import failed",err);toast(elements,`İçe aktarma başarısız: ${err.message}`)}};input.click();}
async function deleteFile(id){const target=files().find(r=>idOf(r)===String(id));if(!target)return;if(!confirm(`“${nameOf(target)}” dosyası silinsin mi?`))return;const before=store.dataSnapshot(),custom=files().filter(r=>idOf(r)!==id);store.replaceData({regions:{...store.get().regions,custom,selectedId:null},campaigns:store.get().campaigns},{recordHistory:false});store.recordHistory(`Dosya silindi: ${nameOf(target)}`,before,store.dataSnapshot());await persist();showFiles();}
function showFiles(){const mode=(()=>{try{return localStorage.getItem("region-console.files-view-mode")==="icons"?"icons":"list"}catch{return"list"}})();const tree=buildTree();const body=`<div class="files-dialog-toolbar"><div class="files-toolbar-row"><input id="filesSearch" class="files-search" type="search" placeholder="Dosya, ülke, il, ilçe, mahalle ara..."/><div class="files-toolbar-actions"><button class="button button-primary" id="sfNew">＋ Yeni dosya</button><button class="button" id="sfImport">⇩ İçe aktar</button></div></div><p class="dialog-muted">Hiyerarşi katalogdan gelir. Her dosya tek bir regions.custom kaydıdır; geometri aynı kaydın içinde tutulur.</p></div><div class="files-tree ${mode==="icons"?"files-tree-icons":""}" id="filesTree">${renderTree(tree)}</div>`;openDialog(elements,"Dosyalar",body);elements.dialogBody.querySelector("#sfNew").onclick=newFile;elements.dialogBody.querySelector("#sfImport").onclick=importFile;elements.dialogBody.querySelector("#filesSearch").oninput=e=>{const q=norm(e.target.value);elements.dialogBody.querySelectorAll(".file-folder").forEach(f=>{f.hidden=!!q&&!norm(f.dataset.folderSearch).includes(q)&&!f.querySelector(`.file-card:not([hidden])`);});elements.dialogBody.querySelectorAll(".file-card").forEach(c=>c.hidden=!!q&&!norm(c.textContent).includes(q));};elements.dialogBody.querySelectorAll(".file-delete").forEach(b=>b.onclick=()=>deleteFile(b.dataset.fileId));}
document.getElementById("filesButton")?.addEventListener("click",showFiles);
export { showFiles };
