import { store } from "../../state/store.js";
import { openDialog, toast } from "../../components/shell.js";
import { BUTTON_PERMISSIONS, LEGACY_PERMISSIONS, can, getAccess, listUsers, createUser, updateUser, createRole, updateRole } from "../../services/rbac.js";

const button = document.getElementById("usersButton");
const dialog = { app: document.getElementById("appDialog"), body: document.getElementById("dialogBody"), title: document.getElementById("dialogTitle") };
const esc = (v) => String(v ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
const session = () => store.get().auth?.session || null;
let access = null; let regionCatalog = []; let lastSessionKey = ""; let loadingAccess = false;

function opts(items, selected = "", placeholder = "Seçiniz") { return `<option value="">${placeholder}</option>` + (items || []).map((x) => `<option value="${esc(x.id)}" ${String(x.id) === String(selected) ? "selected" : ""}>${esc(x.name || x.title || "İsimsiz")}</option>`).join(""); }
function regionsByType(type) { return regionCatalog.filter((region) => String(region.type) === type); }
function childrenOf(parentId, type) { if (!parentId) return []; return regionCatalog.filter((region) => String(region.parent_id || "") === String(parentId) && (!type || String(region.type) === type)); }
function findRegion(id, type = null) { return regionCatalog.find((region) => String(region.id) === String(id) && (!type || String(region.type) === type)) || null; }
function countries() { return regionsByType("country"); }
function countryProvinces(country) { return childrenOf(country?.id, "province"); }
function provinceDistricts(province) { return childrenOf(province?.id, "district"); }
function scopeRow(scope = {}) { return `<div class="rbac-scope-row"><select class="scope-country">${opts(countries(), scope.country_id, "Ülke")}</select><select class="scope-province"><option value="">İl</option></select><select class="scope-district"><option value="">İlçe</option></select><button type="button" class="button rbac-scope-remove" aria-label="Yetki alanını kaldır" title="Yetki alanını kaldır">×</button></div>`; }
function populate(row, scope = {}) { const countrySelect = row.querySelector(".scope-country"); const provinceSelect = row.querySelector(".scope-province"); const districtSelect = row.querySelector(".scope-district"); const countryId = scope.country_id ?? countrySelect.value; const selectedProvinceId = scope.province_id ?? provinceSelect.value; const selectedDistrictId = scope.district_id ?? districtSelect.value; const country = findRegion(countryId, "country"); const provinces = countryProvinces(country); provinceSelect.innerHTML = opts(provinces, selectedProvinceId, "İl"); provinceSelect.disabled = !country || !provinces.length; const province = findRegion(selectedProvinceId, "province"); const districts = provinceDistricts(province); districtSelect.innerHTML = opts(districts, selectedDistrictId, "İlçe"); districtSelect.disabled = !province || !districts.length; }
function read(root) { return [...root.querySelectorAll(".rbac-scope-row")].map((row) => { const countryId = row.querySelector(".scope-country").value; const provinceId = row.querySelector(".scope-province").value; const districtId = row.querySelector(".scope-district").value; if (!countryId && !provinceId && !districtId) return null; const country = findRegion(countryId, "country"); const province = findRegion(provinceId, "province"); const district = findRegion(districtId, "district"); return { country_id: countryId || null, country_name: country?.name || null, province_id: provinceId || null, province_name: province?.name || null, district_id: districtId || null, district_name: district?.name || null }; }).filter(Boolean); }
function wire(root, initial = []) { const list = root.querySelector(".rbac-scopes"); root.querySelector(".rbac-scope-add")?.addEventListener("click", () => { list.insertAdjacentHTML("beforeend", scopeRow()); wireRow(list.lastElementChild); }); initial.forEach((scope) => { list.insertAdjacentHTML("beforeend", scopeRow(scope)); wireRow(list.lastElementChild, scope); }); }
function wireRow(row, scope = {}) { populate(row, scope); row.querySelector(".scope-country").addEventListener("change", () => { row.querySelector(".scope-province").value = ""; row.querySelector(".scope-district").value = ""; populate(row); }); row.querySelector(".scope-province").addEventListener("change", () => { row.querySelector(".scope-district").value = ""; populate(row); }); row.querySelector(".scope-district").addEventListener("change", () => populate(row)); row.querySelector(".rbac-scope-remove").addEventListener("click", () => row.remove()); }
function editor() { return `<fieldset class="rbac-scope-fieldset"><legend>Bölge yetkisi</legend><p class="dialog-muted">Ülke seçildiğinde tüm iller ve ilçeler, il seçildiğinde tüm ilçeler kapsanır.</p><div class="rbac-scopes"></div><button type="button" class="button rbac-scope-add">+ Yetki alanı ekle</button></fieldset>`; }

const GROUPS = [
  { id: "menu", title: "Menü", description: "Kullanıcının görebileceği ana bölümler", open: true, keys: ["button.regions.toggle", "button.campaigns.open", "button.rbac.tab_users", "button.files.open", "button.settings.open"] },
  { id: "tools", title: "Araçlar", description: "Harita üzerinde kullanılabilen işlem araçları", open: true, keys: ["button.tool.draw", "button.tool.edit", "button.tool.delete", "button.tool.import", "button.tool.export", "button.tool.history"] },
  { id: "region", title: "Bölge işlemleri", description: "Bölge bilgi panelindeki işlemler", open: true, keys: ["button.region.create", "button.region.panel.close", "button.region.service.open", "button.region.service.close", "button.region.campaign.manage", "button.region.boundary.edit", "button.region.delete", "button.region.panel.cancel", "button.region.save", "button.region.info.campaign_end"] },
  { id: "campaign", title: "Kampanyalar", description: "Kampanya ekranı ve kampanya işlemleri", open: true, keys: ["button.campaigns.bulk_apply", "button.campaigns.bulk_close", "button.campaigns.create", "button.campaigns.tab.upcoming", "button.campaigns.tab.expired", "button.campaigns.tab.limit", "button.campaigns.tab.active", "button.campaigns.tab.regions", "button.campaigns.edit", "button.campaigns.delete", "button.bulk.select_all", "button.bulk.clear_all", "button.bulk.cancel", "button.bulk.confirm_apply", "button.bulk.confirm_close", "button.campaign_delete.cancel", "button.campaign_delete.confirm", "button.campaign_form.cancel", "button.campaign_form.create", "button.campaign_form.edit", "button.campaign_dialog.cancel", "button.campaign_dialog.apply"] },
  { id: "files", title: "Dosyalar", description: "Dosya görünümü ve dosya işlemleri", open: true, keys: ["button.files.view_list", "button.files.view_icons", "button.files.delete"] },
  { id: "map", title: "Harita", description: "Harita görünümü, katmanlar ve gezinme", open: false, keys: ["button.map.reset", "button.map.layer.standard", "button.map.layer.satellite", "button.map.zoom_out", "button.map.zoom_in", "button.theme.toggle"] },
  { id: "changes", title: "Değişiklikler ve geçmiş", description: "Geri al, ileri al, kaydet ve geçmiş ekranı", open: false, keys: ["button.undo", "button.redo", "button.save", "button.history.simulate", "button.history.back", "button.history.load_more"] },
  { id: "windows", title: "Pencere işlemleri", description: "Açılan onay ve form pencerelerindeki butonlar", open: false, keys: ["button.dialog.close", "button.service_dialog.cancel", "button.service_dialog.confirm", "button.draw.cancel", "button.draw.submit", "button.import.cancel", "button.import.continue"] },
  { id: "rbac", title: "Kullanıcı ve rol yönetimi", description: "Kullanıcı, rol, izin ve bölge kapsamı yönetimi", open: false, keys: ["button.rbac.tab_roles", "button.rbac.create_user", "button.rbac.save_user", "button.rbac.create_role", "button.rbac.save_role", "button.rbac.permission_toggle", "button.rbac.add_scope", "button.rbac.remove_scope"] },
  { id: "footer", title: "Durum özeti", description: "Alt bilgi filtreleri", open: false, keys: ["button.footer.service_filter", "button.footer.campaign_filter", "button.footer.closed_filter"] },
  { id: "session", title: "Oturum", description: "Oturum işlemleri", open: false, keys: ["button.auth.logout"] }
];
const buttonPermissionMap = new Map(BUTTON_PERMISSIONS);
const assigned = new Set(GROUPS.flatMap((group) => group.keys));
const extra = BUTTON_PERMISSIONS.filter(([value]) => !assigned.has(value));
if (extra.length) GROUPS.push({ id: "other", title: "Diğer", description: "Yeni eklenen veya henüz sınıflandırılmamış butonlar", open: false, keys: extra.map(([value]) => value) });

function permissionItem(value, label, selected) { const clean = String(label || value).replace(/^.*?—\s*/, ""); return `<label class="rbac-permission"><input type="checkbox" name="permission" value="${esc(value)}" ${selected.has(value) ? "checked" : ""}><span>${esc(clean)}</span></label>`; }
function permissionGroup(group, selected) { const entries = group.keys.map((key) => buttonPermissionMap.get(key)).filter(Boolean); if (!entries.length) return ""; const checked = entries.filter(([key]) => selected.has(key)).length; const state = checked === entries.length ? "checked" : ""; return `<details class="rbac-permission-group" data-permission-group="${esc(group.id)}" ${group.open ? "open" : ""}><summary><span class="rbac-group-title"><strong>${esc(group.title)}</strong><small>${esc(group.description)}</small></span><span class="rbac-group-meta"><b data-group-count>${checked}/${entries.length}</b><label class="rbac-group-switch" title="Bu bölümdeki tüm izinleri aç/kapat"><input type="checkbox" class="rbac-group-master" ${state}><i></i></label></span></summary><div class="rbac-permission-list">${entries.map(([value, label]) => permissionItem(value, label, selected)).join("")}</div></details>`; }
function perms(selected = []) { const set = new Set(selected); const buttonHtml = GROUPS.map((group) => permissionGroup(group, set)).join(""); const legacyHtml = LEGACY_PERMISSIONS.map(([value, label]) => permissionItem(value, label, set)).join(""); const total = BUTTON_PERMISSIONS.length; const checked = BUTTON_PERMISSIONS.filter(([value]) => set.has(value)).length; return `<div class="rbac-permissions"><div class="rbac-permission-summary"><div><strong>Buton yetkileri</strong><small>Yalnızca kullanıcının gerçekten göreceği ve kullanabileceği işlemleri seçin.</small></div><span><b data-permission-total>${checked}</b> / ${total} açık</span><button type="button" class="button rbac-all-toggle">${checked === total ? "Tümünü kapat" : "Tümünü aç"}</button></div><div class="rbac-permission-groups">${buttonHtml}</div><details class="rbac-advanced-permissions"><summary>Gelişmiş / sistem uyumluluk izinleri <span>${LEGACY_PERMISSIONS.length}</span></summary><p class="dialog-muted">Eski yetki anahtarları backend uyumluluğu için korunur. Normal rol tanımlarken bunları değiştirmeniz gerekmez.</p><div class="rbac-permission-list">${legacyHtml}</div></details></div>`; }
function syncPermissionState(root) { root.querySelectorAll(".rbac-permission-group").forEach((group) => { const boxes = [...group.querySelectorAll("input[name=permission]")]; const master = group.querySelector(".rbac-group-master"); const count = group.querySelector("[data-group-count]"); const checked = boxes.filter((box) => box.checked).length; if (master) { master.checked = boxes.length > 0 && checked === boxes.length; master.indeterminate = checked > 0 && checked < boxes.length; } if (count) count.textContent = `${checked}/${boxes.length}`; }); const all = [...root.querySelectorAll(".rbac-permission-groups input[name=permission]")]; const total = root.querySelector("[data-permission-total]"); const checked = all.filter((box) => box.checked).length; if (total) total.textContent = checked; const allToggle = root.querySelector(".rbac-all-toggle"); if (allToggle) allToggle.textContent = checked === all.length ? "Tümünü kapat" : "Tümünü aç"; }
function wirePermissionGroups(root) { root.querySelectorAll(".rbac-permission-group").forEach((group) => { const master = group.querySelector(".rbac-group-master"); const boxes = [...group.querySelectorAll("input[name=permission]")]; master?.addEventListener("click", (event) => event.stopPropagation()); master?.addEventListener("change", () => { boxes.forEach((box) => { box.checked = master.checked; }); syncPermissionState(root); }); boxes.forEach((box) => box.addEventListener("change", () => syncPermissionState(root))); }); root.querySelector(".rbac-all-toggle")?.addEventListener("click", () => { const boxes = [...root.querySelectorAll(".rbac-permission-groups input[name=permission]")]; const shouldEnable = boxes.some((box) => !box.checked); boxes.forEach((box) => { box.checked = shouldEnable; }); syncPermissionState(root); }); syncPermissionState(root); }
function roleOpts(roles, selected) { return roles.map((role) => `<option value="${esc(role.id)}" ${String(role.id) === String(selected) ? "selected" : ""}>${esc(role.name)} — ${esc(role.description || "")}</option>`).join(""); }
async function ensureAccess(force = false) { const current = session(); const key = current?.access_token && current?.user?.id ? `${current.user.id}:${current.access_token}` : ""; if (!key) { access = null; return null; } if (!force && key === lastSessionKey && access?.loaded) return access; if (loadingAccess) return access; loadingAccess = true; try { access = await getAccess(current.access_token, current.user.id); lastSessionKey = key; return access; } catch (error) { console.error("RBAC management", error); access = null; return null; } finally { loadingAccess = false; } }
async function openManager() { access = window.RegionConsoleRBAC?.access || access; if (!access?.loaded) access = await ensureAccess(true); if (!can(access, "users.manage")) return; const current = session(); if (!current?.access_token) return; openDialog({ appDialog: dialog.app, dialogTitle: dialog.title, dialogBody: dialog.body }, "Yönetim · Kullanıcılar ve Roller", `<p class="dialog-muted">Kullanıcı ve rol yönetimini sade bir şekilde buradan yönetin.</p><div id="rbacManagement"><p class="dialog-muted">Yükleniyor…</p></div>`); try { render(await listUsers(current.access_token)); } catch (error) { dialog.body.innerHTML = `<p class="form-error">${esc(error.message)}</p>`; } }
function render(data) { const { users = [], roles = [], permissionsByRole = {}, scopesByRole = {}, regionCatalog: catalog = [] } = data; regionCatalog = catalog; dialog.body.innerHTML = `<div class="rbac-tabs"><button class="button button-primary" data-tab="users">Kullanıcılar <b>${users.length}</b></button><button class="button" data-tab="roles">Roller <b>${roles.length}</b></button></div><section data-section="users"><div class="rbac-section-intro"><h3>Kullanıcılar</h3><p>Kullanıcıların rolünü ve aktiflik durumunu yönetin.</p></div><form id="createUserForm" class="dialog-form rbac-create-user"><div class="rbac-card-title"><div><strong>Yeni kullanıcı</strong><small>Yeni hesabı oluşturun ve bir rol atayın.</small></div></div><div class="rbac-form-grid"><label>Ad soyad<input name="full_name" required></label><label>E-posta<input name="email" type="email" required></label><label>Geçici şifre<input name="password" type="password" minlength="8" required></label><label>Rol<select name="role_id" required>${roleOpts(roles.filter((role) => role.name !== "super_admin"), roles.find((role) => role.name !== "super_admin")?.id)}</select></label></div><button class="button button-primary" type="submit">Kullanıcı oluştur</button><p class="form-error"></p></form><div class="rbac-user-list"><div class="rbac-list-head"><h3>Mevcut kullanıcılar</h3><span>${users.length} kullanıcı</span></div>${users.map((user) => `<article class="rbac-user-card" data-user-id="${esc(user.id)}"><div class="rbac-user-main"><strong>${esc(user.full_name || user.email)}</strong><small>${esc(user.email)}</small></div><select class="rbac-user-role">${roleOpts(roles, user.role_id)}</select><label class="rbac-active"><input class="rbac-user-active" type="checkbox" ${user.is_active ? "checked" : ""}> Aktif</label><button class="button rbac-user-save" type="button">Kaydet</button></article>`).join("")}</div></section><section data-section="roles" hidden><div class="rbac-section-intro"><div><h3>Roller</h3><p>Bir rol; <b>işlev izinleri</b> ve <b>bölge kapsamından</b> oluşur. Önce rolü seçin, sonra ne yapabileceğini ve nerede yapabileceğini belirleyin.</p></div><span class="rbac-role-count">${roles.filter((role) => role.name !== "super_admin").length} rol</span></div><form id="createRoleForm" class="dialog-form rbac-role-create"><div class="rbac-card-title"><div><strong>Yeni rol oluştur</strong><small>Rol adı, izinler ve bölge kapsamını tek adımda tanımlayın.</small></div></div><div class="rbac-form-grid"><label>Rol adı<input name="name" placeholder="Örn. Bölge Sorumlusu" required></label><label>Açıklama<input name="description" placeholder="Rolün ne yapabildiğini kısa açıklayın" required></label></div><fieldset><legend>İzinler</legend>${perms()}</fieldset>${editor()}<button class="button button-primary" type="submit">Rolü oluştur</button><p class="form-error"></p></form><div class="rbac-role-list"><div class="rbac-list-head"><h3>Mevcut roller</h3><span>Bir role tıklayarak düzenleyin</span></div>${roles.filter((role) => role.name !== "super_admin").map((role) => { const selected = permissionsByRole[role.id] || []; const count = selected.filter((value) => buttonPermissionMap.has(value)).length; const scopeCount = (scopesByRole[role.id] || []).length; return `<details class="rbac-role-card"><summary><span class="rbac-role-name"><strong>${esc(role.name)}</strong><small>${esc(role.description || "Açıklama yok")}</small></span><span class="rbac-role-badges"><b>${count} izin</b><b>${scopeCount} kapsam</b><i>›</i></span></summary><form class="dialog-form rbac-role-form" data-role-id="${esc(role.id)}"><div class="rbac-form-grid"><label>Rol adı<input name="name" value="${esc(role.name)}" required></label><label>Açıklama<input name="description" value="${esc(role.description || "")}" required></label></div><fieldset><legend>İzinler</legend>${perms(selected)}</fieldset>${editor()}<button class="button button-primary" type="submit">Değişiklikleri kaydet</button><p class="form-error"></p></form></details>`; }).join("")}</div></section>`; dialog.body.querySelectorAll("[data-tab]").forEach((tab) => tab.addEventListener("click", () => { dialog.body.querySelectorAll("[data-section]").forEach((section) => { section.hidden = section.dataset.section !== tab.dataset.tab; }); dialog.body.querySelectorAll("[data-tab]").forEach((item) => item.classList.toggle("button-primary", item === tab)); })); dialog.body.querySelectorAll(".rbac-role-form").forEach((form) => { wirePermissionGroups(form); wire(form, scopesByRole[form.dataset.roleId] || []); form.addEventListener("submit", (event) => { event.preventDefault(); saveRole(form); }); }); const createRoleForm = dialog.body.querySelector("#createRoleForm"); wirePermissionGroups(createRoleForm); wire(createRoleForm); createRoleForm.addEventListener("submit", (event) => { event.preventDefault(); saveCreateRole(createRoleForm); }); const createUserForm = dialog.body.querySelector("#createUserForm"); createUserForm.addEventListener("submit", (event) => { event.preventDefault(); saveCreateUser(createUserForm); }); dialog.body.querySelectorAll(".rbac-user-save").forEach((saveButton) => saveButton.addEventListener("click", () => saveUser(saveButton))); }
async function saveCreateRole(form) { const button = form.querySelector("button[type=submit]"); const error = form.querySelector(".form-error"); const payload = Object.fromEntries(new FormData(form).entries()); payload.permissions = [...form.querySelectorAll("input[name=permission]:checked")].map((input) => input.value); payload.scopes = read(form); button.disabled = true; error.textContent = ""; try { await createRole(session().access_token, payload); toast({ toast: document.getElementById("toast") }, "Rol ve bölge kapsamı oluşturuldu."); window.dispatchEvent(new CustomEvent("region-console:rbac-refresh")); render(await listUsers(session().access_token)); } catch (exception) { error.textContent = exception.message; } finally { button.disabled = false; } }
async function saveRole(form) { const button = form.querySelector("button[type=submit]"); const error = form.querySelector(".form-error"); const payload = Object.fromEntries(new FormData(form).entries()); payload.role_id = form.dataset.roleId; payload.permissions = [...form.querySelectorAll("input[name=permission]:checked")].map((input) => input.value); payload.scopes = read(form); button.disabled = true; error.textContent = ""; try { await updateRole(session().access_token, payload); toast({ toast: document.getElementById("toast") }, "Rol ve bölge yetkileri güncellendi."); window.dispatchEvent(new CustomEvent("region-console:rbac-refresh")); render(await listUsers(session().access_token)); } catch (exception) { error.textContent = exception.message; } finally { button.disabled = false; } }
async function saveCreateUser(form) { const button = form.querySelector("button[type=submit]"); const error = form.querySelector(".form-error"); const payload = Object.fromEntries(new FormData(form).entries()); button.disabled = true; error.textContent = ""; try { await createUser(session().access_token, payload); toast({ toast: document.getElementById("toast") }, "Kullanıcı oluşturuldu."); render(await listUsers(session().access_token)); } catch (exception) { error.textContent = exception.message; } finally { button.disabled = false; } }
async function saveUser(saveButton) { const card = saveButton.closest("[data-user-id]"); saveButton.disabled = true; try { await updateUser(session().access_token, { user_id: card.dataset.userId, role_id: card.querySelector(".rbac-user-role").value, is_active: card.querySelector(".rbac-user-active").checked }); toast({ toast: document.getElementById("toast") }, "Kullanıcı güncellendi."); } catch (exception) { toast({ toast: document.getElementById("toast") }, exception.message); } finally { saveButton.disabled = false; } }
button?.addEventListener("click", (event) => { event.preventDefault(); event.stopImmediatePropagation(); openManager(); }, true);
store.subscribe(() => { const current = session(); const key = current?.access_token && current?.user?.id ? `${current.user.id}:${current.access_token}` : ""; if (key && key !== lastSessionKey) ensureAccess(false); });
window.addEventListener("region-console:rbac-updated", () => { access = window.RegionConsoleRBAC?.access || access; });
ensureAccess(false);
